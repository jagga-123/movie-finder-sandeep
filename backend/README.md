# Movie Finder - Backend

No backend is required for this project assessment. 

The Movie Discovery App is built as a frontend-only application using Next.js, and it communicates directly with the TMDB public API.

To run the application, navigate to the `../frontend` directory and follow the instructions in the frontend README.
