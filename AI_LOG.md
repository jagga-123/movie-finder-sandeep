# AI Log

## 1. Tools Used
- `list_dir`: Used to inspect the workspace structure and confirm setup outcomes.
- `run_command`: Used to execute CLI helpers such as Next.js installation and build verification.
- `search_web`: Used to find TMDB keyless usage constraints and create-next-app non-interactive flags.
- `view_file`: Used to read default files generated by create-next-app.
- `write_to_file`: Used to create components, styles, environment files, and metadata.
- `replace_file_content`: Used to apply layout, checklist, and metadata updates cleanly.

## 2. Best Prompts
- **"create-next-app CLI options disable tailwind"**
  - *Explanation:* This search query helped clarify how to initialize Next.js in non-interactive mode while disabling the default Tailwind CSS config, preventing installation of unwanted styling frameworks.
- **"TMDB free public api keyless or demo key"**
  - *Explanation:* This helped confirm that TMDB has no keyless endpoints, guiding us to implement a proper warning message and environment variable lookup (`NEXT_PUBLIC_TMDB_API_KEY`) for token authentication.
- **"Formula to slice custom page results from API paging"**
  - *Explanation:* Refined the calculations needed to map TMDB's fixed 20-item pages to the application's strict 12-item pagination constraint.

## 3. What I Fixed Manually
- **Pagination Boundary Slicing:** Manually calculated indices and implemented concurrent `Promise.all` fetching in `src/services/tmdb.js` to reconcile TMDB's 20-results-per-page with the required 12-results-per-page layout.
- **Heart Button Event Bubbling:** Added `e.stopPropagation()` in `MovieCard.jsx` to prevent the movie details modal from opening when toggling a movie as favorite.
- **Next.js Font Hydration Mismatch:** Cleaned up the default Vercel Geist fonts in `layout.js` and imported `Plus_Jakarta_Sans` from `next/font/google` to properly hook up premium typography.
